#include<bits/stdc++.h>	
using namespace std;
int bin(int n){
	int count=0;
	if(n>1)	bin(n/2);
	if(n%2)	count++;
	return count;
}
int main(){
	//freopen("input.txt","r",stdin);	//freopen("output.txt","w",stdout);
	int n;	cin>>n;
	cout<<bin(n)<<endl;
    return 0;
}

